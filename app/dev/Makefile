# Terraform commands for the app (dev) layer.
# Run from this directory AFTER core-infra/dev is applied (it reads core's
# outputs via remote state), e.g.: make init && make apply
include ../../common.mk
